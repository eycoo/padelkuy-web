# PHP test runner (download with: curl -L https://phar.phpunit.de/phpunit-10.phar -o phpunit.phar)
phpunit.phar

# Composer (if introduced later)
/vendor/

# PHPUnit
.phpunit.result.cache

# Local env overrides
.env
